Place the actual icon image files here so the app and IDE can retrieve them.

Files expected (replace the .txt placeholders with the real PNG images):
- sea-logo.png  -> blue/teal wave logo
- fields-logo.png -> green hills logo

To replace with the attached PNGs, copy the attachment files into this folder and ensure they are named exactly as above.

Examples:
- frontend/public/images/sea-logo.png
- frontend/public/images/fields-logo.png

After adding the images, you can reference them in the app as `/images/sea-logo.png` and `/images/fields-logo.png`.
