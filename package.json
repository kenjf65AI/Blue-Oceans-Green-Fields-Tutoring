{
  "name": "blue-oceans-green-fields-tutoring",
  "version": "1.0.0",
  "description": "AI Productivity Tutor - Professional skill development platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "npm run start --workspace=frontend & npm run start --workspace=backend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "keywords": [
    "ai-tutor",
    "productivity",
    "learning",
    "mentorship"
  ],
  "author": "Blue Oceans & Green Fields",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
